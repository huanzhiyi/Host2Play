name: Host2Play Auto Renew (Playwright)

on:
  schedule:
    # 每天运行一次，北京时间 10:00 (UTC 02:00)
    - cron: '0 2 * * *'
  workflow_dispatch:  # 允许手动触发

jobs:
  renew:
    runs-on: ubuntu-latest
    
    steps:
      - name: Free Disk Space
        run: |
          echo "清理前磁盘空间:"
          df -h
          
          echo "删除不必要的文件..."
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /opt/ghc
          sudo rm -rf /opt/hostedtoolcache/CodeQL
          sudo docker image prune --all --force
          
          echo "清理后磁盘空间:"
          df -h
      
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          lfs: true  # 启用 Git LFS 支持
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: 'requirements_playwright.txt'
      
      - name: Install system dependencies (minimal)
        run: |
          sudo apt-get update
          sudo apt-get install -y wget
      
      - name: Cache Playwright browsers
        uses: actions/cache@v3
        id: playwright-cache
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-chromium
      
      - name: Cache YOLO model
        uses: actions/cache@v3
        id: yolo-model-cache
        with:
          path: model.onnx
          key: yolo-model-onnx-v1
      
      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          # 分步安装以节省空间
          pip install playwright camoufox browserforge requests --no-cache-dir
          pip install ultralytics opencv-python-headless pillow numpy onnxruntime --no-cache-dir
      
      - name: Install Playwright browsers
        if: steps.playwright-cache.outputs.cache-hit != 'true'
        run: |
          playwright install chromium --with-deps
      
      - name: Install Playwright dependencies only
        if: steps.playwright-cache.outputs.cache-hit == 'true'
        run: |
          playwright install-deps chromium
      
      - name: Download YOLO model
        if: steps.yolo-model-cache.outputs.cache-hit != 'true'
        run: |
          echo "🔍 缓存未命中，需要下载 model.onnx..."
          
          # 删除可能存在的 LFS 指针文件
          rm -f model.onnx
          
          echo "📥 从外部源下载 YOLO 模型..."
          
          # 尝试从 GitHub 下载（使用 media 链接绕过 LFS）
          echo "🔄 尝试方法 1: GitHub LFS media 链接..."
          curl -L -o model.onnx "https://media.githubusercontent.com/media/daijro/RecaptchaV2-IA-Solver/main/model.onnx" && {
            FILE_SIZE=$(stat -c%s model.onnx)
            if [ $FILE_SIZE -gt 1000000 ]; then
              echo "✅ 下载成功！文件大小: $(echo "scale=2; $FILE_SIZE/1024/1024" | bc) MB"
              exit 0
            fi
          }
          
          echo "⚠️ 方法 1 失败，尝试方法 2: 直接 GitHub API..."
          rm -f model.onnx
          curl -L -o model.onnx "https://github.com/daijro/RecaptchaV2-IA-Solver/raw/main/model.onnx" && {
            FILE_SIZE=$(stat -c%s model.onnx)
            if [ $FILE_SIZE -gt 1000000 ]; then
              echo "✅ 下载成功！文件大小: $(echo "scale=2; $FILE_SIZE/1024/1024" | bc) MB"
              exit 0
            fi
          }
          
          echo "⚠️ 方法 2 失败，尝试方法 3: Git LFS pull..."
          rm -f model.onnx
          git lfs install
          git lfs pull --include="model.onnx" && {
            if [ -f model.onnx ]; then
              FILE_SIZE=$(stat -c%s model.onnx)
              if [ $FILE_SIZE -gt 1000000 ]; then
                echo "✅ Git LFS 拉取成功！文件大小: $(echo "scale=2; $FILE_SIZE/1024/1024" | bc) MB"
                exit 0
              fi
            fi
          }
          
          echo "❌ 所有下载方法均失败，YOLO 检测将不可用"
          ls -lh model.onnx 2>/dev/null || echo "文件不存在"
      
      - name: Verify YOLO model
        run: |
          echo "🔍 验证 model.onnx 文件..."
          
          if [ -f model.onnx ]; then
            FILE_SIZE=$(stat -c%s model.onnx)
            echo "📦 文件大小: $FILE_SIZE bytes ($(echo "scale=2; $FILE_SIZE/1024/1024" | bc) MB)"
            
            if [ $FILE_SIZE -lt 1000000 ]; then
              echo "❌ 错误: 文件大小异常，可能是 LFS 指针文件"
              echo "📄 文件内容:"
              head -n 3 model.onnx
              exit 1
            else
              echo "✅ 模型文件验证通过"
            fi
          else
            echo "❌ 错误: model.onnx 文件不存在"
            exit 1
          fi
      
      - name: Test YOLO import
        run: |
          python tmp_rovodev_test_yolo_import.py
      
      - name: Run auto renew script
        env:
          RENEW_URL: ${{ secrets.RENEW_URL }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          CI: 'true'
        run: |
          python host2play_auto_renew.py
      
      - name: Upload success screenshot
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: renew-success-${{ github.run_number }}
          path: host2play_renew_success.png
          retention-days: 7
          if-no-files-found: ignore
      
      - name: Upload error screenshot
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: renew-error-${{ github.run_number }}
          path: |
            host2play_error*.png
            host2play_*.png
          retention-days: 7
          if-no-files-found: ignore
      
      - name: 清理工作流记录
        if: always()
        uses: Mattraks/delete-workflow-runs@v2
        with:
          token: ${{ github.token }}
          repository: ${{ github.repository }}
          delete_workflow_pattern: ${{ github.workflow }}
          retain_days: 0
          keep_minimum_runs: 3
